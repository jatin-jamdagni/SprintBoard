{
  "name": "@repo/db",
  "module": "index.ts",
  "type": "module",
  "private": "true",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "echo 'db: no dev server'",
    "typecheck": "tsc --noEmit",
    "db:generate": "bun drizzle-kit generate",
    "db:migrate": "bun run src/migrate.ts",
    "db:seed": "bun run src/seed.ts",
    "db:studio": "bun drizzle-kit studio"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "@types/node": "latest",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "bun-types": "^1.3.11",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  }
}
